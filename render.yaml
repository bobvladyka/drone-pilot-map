services:
  - type: web
    name: drone-pilot-map
    env: node
    plan: free            # nebo standard/starter – dle potřeby
    buildCommand: "npm ci"
    startCommand: "node server.js"
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production